#!/bin/bash
echo Input the number of chips you wanna use, bro
read CHIPSDESIRED

#decide how to add the starting file

#sed will not need to be used until the 2nd iteration
#I'll leave it here for now
sed -n '$LINESTARTNUM, $LINEENDNUM p' origFile.txt > nextFile.txt

